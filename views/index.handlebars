<h1>EAT DA BURGER!</h1>

<h2>Burgers that need to be eaten!</h2>

<ul>
  {{#each burgers}}
    {{#unless eaten}}
      {{> burgers/burger-type eaten=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been eaten!</h2>

<ul>
  {{#each burgers}}
    {{#if eaten}}
      {{> burgers/burger-type eaten=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bg">Burger Name:</label>
    <input type="text" id="bg" name="name">
  </div>

  <div class="form-group">
    <label for="eat">Already Eaten?</label><br>
    <input type="radio" name="eaten" value="1"> Eaten!<br>
    <input type="radio" name="eaten" value="0"> Need to Eat!
  </div>

  <button type="submit">Add Burger</button>
</form>
